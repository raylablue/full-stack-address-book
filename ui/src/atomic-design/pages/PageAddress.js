import React from 'react';
import {Container} from "@mui/material";

const PageAddress = () => {
    return (
        <Container>
            An individual address page
        </Container>
    );
};

export default PageAddress;